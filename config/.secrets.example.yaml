# ============================================================================
# 密钥文件 —— 已在 .gitignore 忽略，不会进 git
# ============================================================================
# 复制此文件为 config/.secrets.yaml，填入真实 key 即可生效。
# 优先级高于主配置里的 apiKey 字段。
# ============================================================================

providers:
  glm:
    apiKey: ""               # ← 在这里填你的智谱 GLM Coding Plan API Key
  # local fallback 一般不需要 key（Ollama 不校验）；保留为占位
  # local:
  #   apiKey: "ollama"
  # anthropic:
  #   apiKey: ""
  # openai:
  #   apiKey: ""
